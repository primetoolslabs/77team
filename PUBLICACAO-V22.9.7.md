# Publicação da V22.9.7

```bash
firebase deploy --only firestore:rules,storage,hosting
```

Depois, entre uma vez como DEV para executar a migração segura dos registros antigos para UID e atualize o navegador com `Ctrl + Shift + R`.
