# Publicação da V22.9.6

Publique o pacote completo:

```bash
firebase deploy --only firestore:rules,storage,hosting
```

Depois da publicação, entre uma vez como DEV para garantir a sincronização de XP existente e use `Ctrl + Shift + R` no navegador.
