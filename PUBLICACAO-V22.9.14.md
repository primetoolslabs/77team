# Publicação da V22.9.14

Projeto Firebase esperado: `team-f78cd`.

```bash
firebase use team-f78cd
firebase deploy --only firestore:rules,hosting
```

Após publicar, atualize o navegador com `Ctrl+Shift+R`.
