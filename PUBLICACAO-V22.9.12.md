# Publicação da V22.9.12

Projeto Firebase esperado: `team-f78cd`.

```bash
firebase use team-f78cd
firebase deploy --only firestore:rules,hosting
```

Após publicar, entre com o DEV e aguarde a mensagem de reconciliação. Se houver nicknames duplicados, altere cada conta pendente para um nickname livre. Depois faça uma atualização forçada no navegador (`Ctrl+Shift+R`).
