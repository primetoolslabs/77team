# Publicação da V22.9.13

Projeto Firebase esperado: `team-f78cd`.

```bash
firebase use team-f78cd
firebase deploy --only firestore:rules,hosting
```

Após publicar, entre com o DEV para sincronizar progressões legadas e atualize o navegador com `Ctrl+Shift+R`.
